# Database connection (injected by Coolify / Docker)
DB_HOST=your-mysql-host
DB_NAME=atlas_kb
DB_USER=atlas
DB_PASS=your_secure_password

# App URL (no trailing slash)
APP_URL=https://kb.yourdomain.com
